{
  "name": "shieldblock-pro",
  "version": "1.0.0",
  "description": "Professional ad & tracker blocker Chrome/Brave extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build:rules": "node scripts/build-rules.js",
    "build:icons": "node scripts/generate-icons.js",
    "build": "node scripts/build-rules.js",
    "dev": "echo 'Load the extension from this directory in Chrome DevTools > Extensions > Load unpacked'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["chrome-extension", "ad-blocker", "privacy", "manifest-v3"],
  "license": "MIT"
}
